import RenderSet from "@/components/set";
import "../global.css";

// This is the Home Page that _layout render when the application starts
export default function Index() {
  // We call the RenderSet component to render the Home Page
  return <RenderSet />;
}
